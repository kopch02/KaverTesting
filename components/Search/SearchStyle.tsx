import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  search: {
    // borderWidth: 1,
    borderRadius: 10,
    width: '100%',
    padding: 10,
    paddingLeft: 20,
    fontSize: 17,
  },
});
